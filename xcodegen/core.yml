name: Core

include:
    - settings.yml

options:
    bundleIdPrefix: me.alxyzhv
    developmentLanguage: base
    groupSortPosition: top
    usesTabs: false
    indentWidth: 4
    tabWidth: 4
    deploymentTarget:
        iOS: 13.0

targets:
    Core:
        platform: iOS
        type: library.static
        sources: [Core]
