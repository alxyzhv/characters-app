name: UI

include:
    - settings.yml

options:
    bundleIdPrefix: me.alxyzhv
    developmentLanguage: base
    groupSortPosition: top
    usesTabs: false
    indentWidth: 4
    tabWidth: 4
    deploymentTarget:
        iOS: 13.0

targets:
    UI:
        platform: iOS
        type: library.static
        sources: [UI]
